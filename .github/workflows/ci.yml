name: CI & Publish
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  ci-publish:
    uses: spike-land-ai/.github/.github/workflows/ci-publish.yml@main
    with:
      has-build: false
      has-tests: false
